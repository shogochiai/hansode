<%= form_for(@estimate_item) do |f| %>
  <% if @estimate_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estimate_item.errors.count, "error") %> prohibited this estimate_item from being saved:</h2>

      <ul>
      <% @estimate_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :estimate_id %><br>
    <%= f.number_field :estimate_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
